# Keep SSH operational data local by default

EZ Tunnel will not collect usage analytics or transmit SSH logs, host aliases, destinations, or port mappings. Crash reporting may be offered only as an explicit opt-in and must redact SSH command arguments, filesystem paths, and connection metadata before transmission; aside from checking the signed application update feed, the default installation sends no data from the user's machine.
